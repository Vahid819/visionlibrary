import AddStudentForm from "@/components/student-form/AddStudentForm";

export const metadata = {
  title: "Add Student — Vision Library",
  description: "Register a new student in Vision Library",
};

export default function Page() {
  return <AddStudentForm />;
}
